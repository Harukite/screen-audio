// Entry point for the ScreenAudio menu bar app.
// Actual app/bootstrap code will be added in later tasks.

import ScreenAudioCore

// No-op main: the real app lifecycle (menu bar UI, audio engine) lands in later tasks.
